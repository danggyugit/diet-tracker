"""📖 사용 메뉴얼 — 앱 기능별 상세 가이드."""

import streamlit as st

from services.auth_service import require_auth

require_auth()

st.title("📖 사용 메뉴얼")
st.caption("기능별 상세 가이드 — 처음 사용자도 3분 안에 시작할 수 있습니다.")

# ═══════════════════════════════════════════════════════════════
# 빠른 시작
# ═══════════════════════════════════════════════════════════════

st.markdown(
    """
    <div style='background:linear-gradient(135deg,rgba(34,197,94,0.15),rgba(59,130,246,0.15));
                border:1px solid rgba(34,197,94,0.3);border-radius:14px;padding:18px;margin:8px 0 20px;'>
        <div style='font-size:18px;font-weight:700;margin-bottom:8px;'>🚀 3분 빠른 시작</div>
        <div style='font-size:14px;color:#CBD5E1;line-height:1.8;'>
            <b>1단계</b> — 📱 아래 <b>"홈 화면에 추가"</b> 로 앱 바로가기 설치 <b>(강력 추천)</b><br>
            <b>2단계</b> — 👤 프로필에서 신체정보·활동수준·감량강도 입력 <b>(필수)</b><br>
            <b>3단계</b> — 🍽️ 오늘 먹은 음식 기록 → 📅 📊 추세 확인
        </div>
    </div>
    """,
    unsafe_allow_html=True,
)

# ═══════════════════════════════════════════════════════════════
# 모바일 홈 화면 추가 (최상단 강조)
# ═══════════════════════════════════════════════════════════════

with st.expander("📱 홈 화면에 추가 — 매일 쓰려면 필수! (Android · iOS)", expanded=True):
    st.markdown(
        """
        <div style='background:rgba(59,130,246,0.1);border-left:3px solid #3B82F6;padding:10px 14px;border-radius:6px;margin-bottom:12px;'>
            <b>왜 추가해야 하나요?</b><br>
            매일 먹은 음식을 기록하려면 <b>1초 접근</b>이 중요합니다.
            홈 화면 아이콘으로 만들면 브라우저를 여는 번거로움이 사라져 습관으로 정착하기 훨씬 쉽습니다.
        </div>
        """,
        unsafe_allow_html=True,
    )
    st.markdown(
        """
        **🤖 Android (Chrome · Samsung Internet · Edge)**
        1. 모바일 Chrome 브라우저로 이 앱 접속
        2. 우측 상단 **⋮ (메뉴)** 아이콘 탭
        3. **"홈 화면에 추가"** 선택
        4. 이름 확인 후 **[추가]** → 홈 화면에 아이콘 생성
        5. 홈 화면 아이콘 탭 → 바로 실행

        ---

        **🍎 iOS (Safari 필수)**
        1. **Safari** 로 이 앱 접속 (⚠️ Chrome·타 브라우저는 메뉴 없음)
        2. 하단 **공유 버튼** (⬆️ 네모+화살표) 탭
        3. 메뉴 스크롤 → **"홈 화면에 추가"** 선택
        4. 이름 확인 후 **[추가]** 탭
        5. 홈 화면 아이콘 탭 → 바로 실행

        ---

        **💡 설치 후 장점**
        - 홈 화면 아이콘으로 **1초 접근**
        - 로그인 상태가 유지되어 매번 로그인할 필요 없음
        - 알림/즐겨찾기처럼 **매일 사용 루틴 형성**에 도움

        ---

        **문제 해결**
        - **메뉴가 안 보여요** → 브라우저를 최신 버전으로 업데이트하거나 시크릿 모드 해제
        - **아이콘이 깨져 보여요** → 삭제 후 다시 추가
        - **iOS에서 메뉴가 없어요** → 반드시 **Safari** 에서 접속해야 합니다
        """
    )

# ═══════════════════════════════════════════════════════════════
# 프로필
# ═══════════════════════════════════════════════════════════════

with st.expander("👤 프로필 — 개인 목표 설정 (가장 먼저 할 일)", expanded=True):
    st.markdown(
        """
        **왜 먼저 설정해야 하나요?**
        프로필 정보를 기반으로 **BMR·TDEE·일일 목표 칼로리·영양소(탄/단/지)**가 자동 계산됩니다.
        이 값이 모든 페이지의 기준이 되므로 가장 먼저 입력해 주세요.

        ---

        **입력 항목**
        - **성별·나이·키·체중** — BMR 계산 (Mifflin-St Jeor 공식)
        - **활동 수준** — 비활동(1.2) / 가벼운활동(1.375) / 보통활동(1.55) / 활발한활동(1.725) / 매우활발(1.9)
        - **감량 강도** — 🟢 가벼운(-500) / 🟡 보통(-700) / 🔴 강한(-1000) / ⚪ 유지
        - **목표 체중·날짜** — 예측선 계산용 (선택)

        ---

        **자동 계산 결과**
        - **BMR** = 기초대사량 (쉴 때 쓰는 칼로리)
        - **TDEE** = BMR × 활동계수 = 하루 총 소모
        - **일일 목표** = TDEE − 감량 강도
        - **단백질** = 체중 × 1.3~1.8g (활동 수준별)
        - **지방** = 목표 칼로리의 30%
        - **탄수화물** = 나머지

        ---

        **💡 팁**
        - 체중이 바뀌면 BMR·영양 목표가 자동 갱신됩니다. 주 1회는 업데이트하세요.
        - 감량 강도는 **🟡 보통(-700)** 을 권장 — 꾸준히 가능하고 요요 위험이 낮습니다.
        - 활동 수준은 **운동량 기준이 아니라 전체 생활 활동량** 입니다. 헬스 주 3회 + 사무직이면 "가벼운활동".
        """
    )

# ═══════════════════════════════════════════════════════════════
# 식단 기록
# ═══════════════════════════════════════════════════════════════

with st.expander("🍽️ 식단 기록 — 3가지 입력 방법"):
    st.markdown(
        """
        **방법 1: 📷 AI 사진 분석**
        1. "사진 업로드" 탭 선택
        2. 음식 사진 업로드 (카메라 촬영 or 갤러리)
        3. **[분석]** 버튼 → Gemini AI가 음식명·칼로리·영양소 자동 추출
        4. 결과 확인·수정 후 **[저장]**

        ⚠️ 여러 음식이 섞인 사진도 분석 가능하지만 **개별 촬영이 가장 정확**합니다.

        ---

        **방법 2: ✍️ 직접 입력 (AI 영양소 추정)**
        1. "직접 입력" 탭 선택
        2. 음식 이름 입력 (예: "된장찌개")
        3. **[AI 영양 추정]** 버튼 → 한국 표준 1인분 기준 자동 채움
        4. 칼로리·영양소·양·인분 수 자유롭게 수정
        5. **[식단에 추가]**

        💡 브랜드 음식(스타벅스 아메리카노 등)도 입력 가능합니다.

        ---

        **방법 3: ⭐ 즐겨찾기 / 📋 어제 복사 (가장 빠름)**
        - **즐겨찾기 원터치** — 자주 먹는 음식 클릭 1번으로 바로 추가
        - **어제 복사** — "어제 식단 가져오기" 버튼 → 동일 메뉴 반복 시 3초 입력
        - 즐겨찾기는 **설정 > ⭐ 즐겨찾기**에서 관리하거나, 식사 기록에서 자동 누적됩니다.

        ---

        **식사 구분**
        - 🌅 아침 / ☀️ 점심 / 🌙 저녁 / 🍪 간식
        - 날짜·식사 구분을 먼저 선택한 뒤 음식을 추가합니다.

        ---

        **💡 팁**
        - 수량은 **0.5 단위** 로 조정 가능 (예: 0.5인분, 1.5인분)
        - "양" 필드는 자유 입력 (예: "1인분", "200g", "큰 사발")
        - 저장 후에도 수정·삭제 가능
        """
    )

# ═══════════════════════════════════════════════════════════════
# 운동·체중
# ═══════════════════════════════════════════════════════════════

with st.expander("🏃 운동 기록 · ⚖️ 체중 기록"):
    st.markdown(
        """
        **🏃 운동 기록**
        1. 식단 기록 페이지 하단 "운동 기록" 섹션
        2. 20종 운동 중 선택 (걷기·달리기·수영·자전거·헬스 등)
        3. **시간(분) 입력** → 체중 기반 MET 공식으로 소모 칼로리 자동 계산
        4. **[추가]** 버튼 → 순 칼로리(섭취 − 소모)에 자동 반영

        ⚠️ MET 값은 중강도 기준입니다. 고강도 운동은 실제보다 적게 계산될 수 있습니다.

        ---

        **⚖️ 체중 기록**
        - 매일 아침 공복 체중을 기록하는 것을 권장
        - 프로필의 "현재 체중" 필드는 **가장 최근 기록이 자동 반영**됩니다
        - 📊 트렌드 페이지에서 **7일 이동평균 + 예측선** 확인 가능

        ---

        **💡 팁**
        - 체중은 매일 변동이 큽니다 (수분·음식량). **주간 평균**으로 보세요.
        - 운동 소모 칼로리는 **보너스** 로 생각하고, 식단 관리가 70% 이상 중요합니다.
        """
    )

# ═══════════════════════════════════════════════════════════════
# 캘린더
# ═══════════════════════════════════════════════════════════════

with st.expander("📅 캘린더 — 월별 한눈에 보기"):
    st.markdown(
        """
        **보는 법**
        - 각 날짜 셀에 **순 칼로리** (섭취 − 소모) 표시
        - 🟢 **초록** : 목표 이하 (OK)
        - 🟡 **노랑** : 목표 10% 이내 초과
        - 🔴 **빨강** : 목표 크게 초과
        - 하단에 "식{칼로리}·운{칼로리}" 세부 표시

        ---

        **네비게이션**
        - 상단 **연/월 드롭다운** — 임의 월로 점프
        - **◀ 이전달 · 다음달 ▶** — 인접 월 이동
        - **가운데 버튼** (현재 연/월) — 오늘 달로 복귀

        ---

        **날짜 상세**
        - 캘린더 아래 "📋 상세 보기할 날짜" 에서 특정 일 선택
        - **매크로 도넛** (탄/단/지 비율 + 총 섭취 칼로리)
        - 컨디션·메모
        - 식사별(아침/점심/저녁/간식) 음식 목록
        """
    )

# ═══════════════════════════════════════════════════════════════
# 트렌드
# ═══════════════════════════════════════════════════════════════

with st.expander("📊 트렌드 — 주간·월간 인사이트"):
    st.markdown(
        """
        **주요 차트**
        - **일별 칼로리 라인** + 목표 기준선
        - **체중 추이** + 7일 이동평균 + 목표 도달 예측선
        - **주간 요약** (평균 섭취·소모·순 칼로리, 목표 달성률)
        - **TOP 5 자주 먹은 음식**
        - **연간 GitHub 스타일 히트맵**
        - **매크로 분포** (오늘 vs 주간 평균)

        ---

        **기간 선택**
        - **7일** / **30일** 전환 가능
        - 최근 활동이 부족하면 "데이터 부족" 안내

        ---

        **💡 팁**
        - 체중 그래프는 **이동평균선** 을 중심으로 추세를 판단하세요.
        - 목표 예측선이 목표 날짜보다 늦어지면 감량 강도를 재조정하세요.
        """
    )

# ═══════════════════════════════════════════════════════════════
# 즐겨찾기
# ═══════════════════════════════════════════════════════════════

with st.expander("⭐ 즐겨찾기 — 자주 먹는 음식 관리"):
    st.markdown(
        """
        **추가 방법**
        - **식사 기록에서 채우기** — 버튼 클릭 → 자동으로 자주 먹은 음식 채움
        - **새 음식 추가** — 수동 입력 + AI 영양 추정
        - 식단 기록 페이지에서 추가할 때 **[즐겨찾기 등록]** 체크박스

        ---

        **활용**
        - 🔍 검색으로 빠르게 찾기
        - ✏️ 수정 · 🗑️ 삭제 버튼
        - **사용 횟수** 가 자동 누적되어 자주 먹는 음식이 상단 정렬

        ---

        **💡 팁**
        - "아메리카노", "현미밥 1공기" 같은 반복 메뉴를 미리 등록해 두세요.
        - 즐겨찾기로 등록된 음식은 식단 기록에서 **원터치 1초 추가** 가능합니다.
        """
    )

# ═══════════════════════════════════════════════════════════════
# 데이터 내보내기
# ═══════════════════════════════════════════════════════════════

with st.expander("📥 데이터 내보내기 · 🗑️ 계정 관리"):
    st.markdown(
        """
        **CSV 내보내기**
        - 프로필 페이지 하단 **📥 내 데이터 내보내기** 섹션
        - 기간 선택 → 식단·체중·운동 각각 CSV 다운로드
        - UTF-8(BOM) 포함 → Excel·Google Sheets 한글 깨짐 없음

        ---

        **로그아웃 / 재로그인**
        - 사이드바 상단 프로필 카드 우측 ⇥ 버튼

        ---

        **💡 팁**
        - 주기적으로 CSV 백업을 권장합니다.
        - 오래된 기록도 언제든 수정 가능합니다 — 캘린더에서 과거 날짜 선택 후 편집.
        """
    )

# ═══════════════════════════════════════════════════════════════
# FAQ
# ═══════════════════════════════════════════════════════════════

with st.expander("❓ 자주 묻는 질문 (FAQ)"):
    st.markdown(
        """
        **Q. AI 분석이 부정확해요.**
        A. 사진이 흐리거나 여러 음식이 섞인 경우 정확도가 떨어집니다. 음식별 개별 촬영 + 조명 밝은 곳에서 촬영하세요. 결과는 언제든 수동 수정 가능합니다.

        ---

        **Q. 목표 칼로리가 너무 낮아요/높아요.**
        A. 프로필의 **감량 강도**를 조정하세요. 너무 낮으면 지속 불가, 너무 높으면 감량 속도가 느립니다. 🟡 보통(-700)을 권장.

        ---

        **Q. 체중이 줄었는데 목표 칼로리가 그대로예요.**
        A. 프로필에서 현재 체중을 업데이트하면 BMR·TDEE가 재계산됩니다. (체중 기록 시 자동 반영)

        ---

        **Q. 운동 소모 칼로리가 너무 높아 보여요.**
        A. MET 공식은 평균값입니다. 고강도 운동은 실제보다 낮게, 저강도는 약간 높게 나올 수 있으니 **±20% 오차** 를 감안하세요.

        ---

        **Q. 모바일 홈 화면에 앱처럼 추가할 수 있나요?**
        A. 네. 메뉴얼 상단의 **📱 홈 화면에 추가** 섹션에서 Android·iOS별 방법을 확인하세요.

        ---

        **Q. 데이터가 사라졌어요.**
        A. Google 로그인 이메일 기준으로 저장되므로, 다른 계정으로 로그인하면 빈 상태로 보입니다. 원래 계정으로 로그인하세요.
        """
    )

st.divider()
st.caption(
    "💬 제안·버그 제보 환영합니다. 이 메뉴얼은 새 기능 추가 시 업데이트됩니다."
)
